# SonicCheck v1.1.0 发布：新增网易云/QQ音乐加密格式解锁 + 12项稳定性修复

> **下载地址：**
> GitHub：https://github.com/GarrettFynn/SonicCheck/releases/tag/v1.1.0
> 备用：发邮件至 feifeifynn@qq.com 索取（打包好的绿色版，解压即用）
>
> ⚠️ **本文较长但信息密度高，建议先收藏再阅读。**

---

## 一、v1.1.0 一句话总结

在 v1.0.0（无损真伪鉴定 + 曲库管理）基础上，新增**加密音乐格式解锁**完整功能链路——网易云 `.ncm`、QQ 音乐 QMC 全系（`.mflac`/`.mgg`/`.qmc*` 等 30+ 扩展名）一键解锁为标准 FLAC/WAV，源文件只读不动。

同时完成一轮**全项目代码审查**，修复 3 个 P1 bug、5 个 P2 问题、4 项 P3 加固，测试扩充到 **101 项断言**，双环境全绿，打包后 123MB 绿色版已推送 GitHub。

---

## 二、新功能详解：加密音乐格式解锁

### 2.1 一句话说明这个功能是干什么的

你下载的网易云 `.ncm`、QQ 音乐 `.mflac` 这些文件，本质上是被加密的音频容器。SonicCheck v1.1.0 可以把它**解密还原成标准格式**（FLAC/WAV），让你能导入 foobar2000、车载音响、HiFi 播放器，或者其他任何不支持加密格式的设备里播放。

**注意**：解密还原的是你已经下载到本地的文件，软件不会帮你绕过付费、不会下载新歌。

### 2.2 支持格式一览

| 平台 | 格式 | 说明 |
|---|---|---|
| 网易云音乐 | `.ncm` | 完整支持，自动提取歌名/歌手/专辑/封面并内嵌输出 |
| QQ 音乐 | `.mflac`、`.mflac0/1/a/h/l/m` | QMC 加密，三种流密码自动分流处理 |
| QQ 音乐 | `.mgg` | 同上 |
| QQ 音乐 | `.qmc0` ~ `.qmc8` | 同上 |
| QQ 音乐 | `.qmcflac`、`.qmcogg` | 同上 |
| QQ 音乐 | `.tkm`、`.bkc*` 等 | 同上 |

**总计 30+ 种扩展名**，基本上你本地能见到的 QQ 音乐加密文件都能处理。

算法层面，NCM 走的是 AES-128-ECB + 自定义 KeyBox XOR 流；QMC 按密钥长度自动分流到三种流密码（map 密码 / 分段 RC4 / static 静态密码盒）。全部逐字节对照 unlock-music CLI v0.2.12 与 qmdec（MIT 许可证）参考实现，**合成文件加密→解密往返验证逐字节一致**。

### 2.3 输出格式三种可选

解锁窗口提供三种输出策略：

**① 自动（推荐）**
保留内层原始格式。如果加密壳里包的是 FLAC，输出就是 FLAC；包的是 MP3，输出就是 MP3。这是**无损重封装**，音质零损失。

**② 强制 FLAC**
统一输出 FLAC 容器。适合播放器兼容性需求。

**③ 强制 WAV**
统一输出 WAV 容器。适合需要纯 PCM 的场景。

> ⚠️ **重要提示**：如果内层是有损格式（MP3/OGG/M4A），强制转 FLAC/WAV **只是换容器，不会提升音质**。软件界面会明确提示这一点，避免"音质提升"的误解。解锁的本质是解密+重封装，不是"音质修复"。

### 2.4 QQ 音乐新版 musicex 加密：密钥不在文件里，怎么办？

这是 v1.1.0 技术含量最高的部分。

#### 问题：新版 QQ 音乐的密钥不在文件内

QQ 音乐新版客户端下载的 `.mflac` 文件末尾有个 `musicex` 标记，但**密钥本身不在文件里**。文件只保存了一个 `song_mid`（歌曲唯一标识），真正的解密密钥需要向腾讯服务器申请。

没有密钥，这个文件就是一个打不开的加密容器。

#### SonicCheck 的解决方案：一键从客户端取密钥

整个链路设计如下：

**Step 1：识别 musicex 尾部**
- 文件末 8 字节 magic：`musicex\0`
- 解析尾块提取 `song_mid` 和客户端原始文件名

**Step 2：本地密钥库查询**
- 路径：`~/.soniccheck/qqmusic_ekeys.json`
- 按 `mid:`（song_mid）和 `file:`（原始文件名）双键索引
- 已缓存的密钥直接命中，无需重复请求

**Step 3：从 QQ 音乐客户端提取登录态**
- 枚举进程找到 `QQMusic.exe`
- Win32 API 只读扫描内存，搜索 `qqmusic_key=` 标记提取 cookie
- 内存扫描区域上限 50MB，不读全量

**Step 4：调用官方接口换密钥**
- 接口：`POST https://u.y.qq.com/cgi-bin/musicu.fcg`
- 使用上一步提取的 cookie 作为登录态
- 逐首歌提交 `song_mid`，换取对应解密密钥

**Step 5：双键落盘缓存**
- 换取成功的密钥写入本地密钥库
- 后续解锁同一批歌直接命中

#### 你需要做什么？

非常简单：
1. **打开 QQ 音乐客户端**
2. **登录下载过这些歌的账号**（需要是 VIP/付费账号）
3. 在 SonicCheck 解锁窗口点"**导入 QQ 音乐密钥**"
4. 等进度跑完，密钥就缓存好了

#### 安全设计

- **cookie 用 Windows DPAPI 加密落盘**：只有当前 Windows 用户能解密，换机/换用户自动失效
- **只读内存扫描**：不修改 QQ 音乐任何数据
- **密钥不上传**：全部本地存储
- **失败自动重试一轮**：实测 7/7 成功率

#### 内置图文教程

怕不会操作？解锁窗口里直接有"**教程**"按钮，点击弹出图文对话框，手把手教。

### 2.5 联网补标签：musicex 文件本身不带歌名

musicex 文件**不携带任何标签信息**——没有歌名、没有歌手、没有封面。解锁后如果直接输出，会得到一个文件名乱糟糟、没有元数据的裸音频文件。

SonicCheck 的解决方案：

**按 song_mid 调官方详情接口**
- 精确取回：歌名、歌手、专辑名
- 封面走图床：`y.gtimg.cn/music/photo_new/T002R500x500M000{album_mid}.jpg`

**标签写入**
- 中文标签走 ffmetadata1 文件（UTF-8 编码），绕开 Windows 命令行 ANSI 乱码
- 封面内嵌到 FLAC/MP3
- 文件名自动清理 `_EM`/`_EG` 等音质尾缀

**关键决策：不用搜索接口**
早期尝试用搜索接口，但会把 `song_mid` 当关键词返回错歌（踩过坑，已定型为详情接口）。

**可关闭**
解锁窗口提供"联网补齐 QQ 音乐标签"开关（默认开），勾选关闭即走纯离线解锁。失败自动降级为无标签输出，不影响解锁本身。

### 2.6 流式解密：300MB 文件只占用 76MB 内存

#### 旧方案的问题

初始实现把整个文件读进内存，峰值内存 ≈ 3× 文件大小：
- 文件数据（1×）
- 解密缓冲区（1×）
- 输出缓冲区（1×）

300MB 文件 → ~900MB 峰值内存，普通用户顶不住。

#### v1.1.0 的流式方案

**NCM**：按 8MB 分块 XOR 解密，keystream 按流内绝对偏移计算，保证跨块连续性。

**QMC**：三种流密码的 keystream 都只依赖绝对偏移，天然支持随机访问分块。

**临时文件管理**：解密产物先写 `unlock_*.bin` 临时文件，同盘 rename 保证原子性，`finally` 兜底清理。

**实测数据**：

| 指标 | 数值 |
|---|---|
| 测试文件 | 296.2 MB「大石碎胸口.ncm」 |
| 峰值内存 | **75.9 MB**（约文件大小的 25.6%） |
| 进度回调 | 36 次，单调递增收尾 100% |

内存占用与文件大小**基本解耦**，解几个 G 的 Hi-Res 文件也不会爆内存。

### 2.7 解锁对话框 UX 细节

**文件列表**
- 添加文件 / 添加文件夹 / 清空
- 自动识别格式，行首打 `[NCM]` / `[QMC]` 前缀

**输出设置**
- 输出目录：默认首个文件同级 `已解锁/`
- 输出格式：自动 / 强制 FLAC / 强制 WAV
- 有损内层转无损容器时明确提示"只是换容器，音质不变"

**密钥操作**
- 「导入 QQ 音乐密钥」按钮
- 「教程」按钮
- 密钥库缓存计数显示
- 「导入后自动开始解锁」开关（默认开）

**进度**
- 状态栏双级进度：整体 `文件 3/13` + 单文件 `—— 45%`（字节级精度）
- 全程可取消，取消后自动清理临时文件

---

## 三、稳定性修复（P1/P2/P3 全记录）

本轮对全部 6524 行 Python（17 个文件）进行逐行精读审查。

### 🔴 P1 真 bug（3 项）

| # | 问题 | 修复 |
|---|---|---|
| 1 | ffmpeg 转码失败留半截输出文件，重跑生成 `xxx (1).flac` 垃圾 | 拆 `_write_output` 为内外两层，外层捕获 `BaseException` 删 dst |
| 2 | 解锁单文件期间无法取消，关窗假死最长 2 分钟 | 流式循环每块检查取消标志；ffmpeg 改 Popen 轮询+kill |
| 3 | 密钥导入无法取消 | 逐首+提取前检查取消标志；取消后带 `cancelled` 标记 |

### 🟡 P2 小 bug（5 项）

| # | 问题 | 修复 |
|---|---|---|
| 4 | 扫描停止后"分析中…"行永久悬挂 | 统一标记「已取消」 |
| 5 | 非安全模式去重后清除项留在结果表，后续重命名误操作 | 移动成功后从表格移除 |
| 6 | 解锁完成行丢失 `[NCM]/[QMC]` 前缀 | 字典保留格式标签 |
| 7 | 120 秒超时对 300MB+ hi-res 不够 | 按 2MB/s 保守速率随大小缩放（300MB→150s） |
| 8 | 歌单短名误配（如「笑」匹配所有含"笑"的文件） | 模糊匹配要求规范化后 ≥2 字符 |

### 🔵 P3 加固（4 项）

| # | 事项 | 实施 |
|---|---|---|
| 9 | cookie 明文落盘风险 | Windows DPAPI 加密 |
| 10 | README 引用的构建文档缺失 | 补齐 build.bat + BUILDING.md |
| 11 | 版本号不一致 | APP_VERSION 提为 1.1.0 |
| 12 | 文件操作零测试 | 新增 `test_file_ops.py`（32 项） |

---

## 四、核心功能回顾（v1.0.0 已有）

### 无损真伪批量鉴定

- **六维评分**：位深 / 采样率 / -60dB 截止频率 / 动态范围 DR / 立体声相关性 / 峰值
- **三条假无损判定规则**：
  - 频谱断崖（15kHz~Nyquist 间 >30dB 落差）
  - -60dB 截止频率 < 16kHz
  - 24bit 但 DR < 4（位深造假）
- **批量扫描**：递归子文件夹，默认 8 线程，几千首歌几分钟
- **可信度**：判假 >95%（物理硬证据），判真 ~85-90%（没找到假证据）

### 曲库管理

- **智能重命名**：自动加 `[真无损]`/`[假无损]` 标记，旧标记自动剥离防套娃
- **标签改名**：按 ID3/Vorbis Comment 规范化文件名
- **去重**：同名对比 + 内容签名去重（时长/DR/相关度/截止频率四指标 + 并查集）
- **歌单导入**：网易云链接零登录全量解析
- **导出 CSV**：UTF-8-BOM，Excel 直接打开

### 安全与易用

- **安全模式**：所有写操作变复制/只读，输出到 `<文件夹名>_安全输出/`
- **拖拽支持**：直接拖文件夹到窗口
- **协作式取消**：扫描/解锁中随时停止，不残留临时文件

---

## 五、下载与使用

### 系统要求

- Windows 10 / 11（64 位）
- 绿色版，解压即用，无需安装

### 下载

GitHub（推荐）：
👉 https://github.com/GarrettFynn/SonicCheck/releases/tag/v1.1.0

备用（邮件索取）：
📧 feifeifynn@qq.com

### 首次使用（5 步）

**Step 1：解压**
下载 `SonicCheck_v1.1.0_win64.zip`（123MB），解压到任意目录。

**Step 2：确认 ffmpeg**
绿色版已内置 ffmpeg。自行从源码运行时，需将 `ffmpeg.exe` 和 `ffprobe.exe` 放入 `resources/ffmpeg/`。

**Step 3：拖文件夹扫描**
打开 `SonicCheck.exe`，拖拽音乐文件夹到窗口，或点"选择文件夹"，点"开始扫描"。

**Step 4：查看结果**
表格显示真无损（绿）/ 假无损（红）/ 分析失败，双击看六维详情。

**Step 5：解锁加密格式**
点左栏"格式解锁"：
1. 添加 `.ncm` / `.mflac` / `.qmc*` 文件
2. QQ 音乐新版文件先点"导入 QQ 音乐密钥"（确保客户端已登录）
3. 点"开始解锁"
4. 收工

### 升级建议

v1.0.0 用户直接覆盖解压即可。QSettings 配置自动保留。

---

## 六、⚠️ 使用边界与合规说明（请务必阅读）

### 6.1 这个工具的精确定位

SonicCheck 是一个**本地音频文件格式转换与质量分析工具**。

- 它处理的是你**已经下载到本地设备**的音频文件
- 它不会帮你绕过付费、不会下载新歌、不会破解平台的付费验证
- 它不会修改原文件，所有输出都是副本

### 6.2 什么是可以的（安全使用）

✅ **转换你自己已付费下载的文件**：你是 VIP/单曲购买/数字专辑购买用户，通过官方客户端下载了加密文件到本地，现在想在自己的设备上播放。

✅ **仅供个人使用**：转换后的文件只存在你自己的电脑、手机、播放器里，自己听。

✅ **格式兼容需求**：你的车载音响、HiFi 播放器、foobar2000 不支持 `.ncm`/`.mflac`，需要转成 FLAC/WAV。

✅ **曲库整理**：扫描检查哪些 FLAC 是假的，去重、重命名、按歌单整理。

### 6.3 什么是不可以的（红线）

❌ **上传、分享、传播解锁后的文件**：发到微信群、QQ 群、网盘、论坛、P2P 网络——即使不收钱，这也可能构成版权侵权。

❌ **把 SonicCheck 当"破解工具"宣传**：它不是破解工具，它不能绕过付费。如果你这样宣传，可能招致平台投诉或法律风险。

❌ **解锁你没有付费权限的文件**：如果你不是 VIP、没有购买这首歌，理论上你本地不会有加密文件。但如果你通过其他途径获得了文件，请确保你有合法的使用权。

❌ **商业利用**：把解锁后的文件用于商业场景（店铺背景音乐、视频配乐等），除非你拥有相应的授权。

### 6.4 关于"不盈利"

SonicCheck 是**完全免费的 GPL v3 开源软件**，作者不以任何形式收费或获取商业利益。

这一点在法律层面意味着什么：

- **降低了版权侵权的严重性**：在版权法"合理使用"判断中，"非营利、个人使用"是重要考量因素
- **刑事风险极低**：各国版权犯罪通常要求"以营利为目的"
- **不代表绝对豁免**：即使不盈利，大规模传播文件仍然可能违法

**简单说：你自己用，没问题。你传播出去，就有问题。**

### 6.5 平台用户协议的说明

网易云音乐和 QQ 音乐的用户协议中，通常包含"不得反向工程、破解技术措施"等条款。

- 这些条款属于**合同约束**，违反可能导致账号被封禁
- 但 SonicCheck **不连接平台服务器**，不会在平台上留下可追踪的日志
- 平台的主动监测主要针对**上传和传播行为**，而非本地文件处理

**降低账号风险的建议**：
- 不要在平台社区中公开讨论你使用了格式转换工具
- 不要将从平台下载的文件重新上传回平台
- 保留你的 VIP 订阅/购买记录

### 6.6 技术层面的安全保障

SonicCheck 在设计上做了以下安全考量：

| 设计 | 说明 |
|---|---|
| **源文件只读** | 解锁/鉴定全程不动原文件，输出撞名加序号 `(n)`，不覆盖 |
| **cookie DPAPI 加密** | QQ 音乐登录态用 Windows DPAPI 加密，仅当前用户可解 |
| **密钥不上传** | 所有密钥本地存储，不出本机 |
| **零黑箱** | 源码完全公开，6000+ 行 Python 可审计 |
| **零新增依赖** | 解锁算法全部纯 Python 实现，没有供应链风险 |

### 6.7 免责声明摘要

本软件按"原样"提供，分析结果基于频谱特征和统计指标，仅供参考。作者不对因使用本软件导致的任何数据丢失或文件损坏承担责任。建议在安全模式下首次使用，或先备份重要数据。

完整法律免责声明见 GitHub 仓库根目录 `LEGAL_DISCLAIMER.md`。

---

## 七、技术背书

### 开源可审计

- **许可证**：GPL v3
- **源码**：GitHub 完全公开，6000+ 行 Python
- **算法参考**：unlock-music CLI（MIT）、qmdec（MIT），兼容 GPL v3

### 101 项自动化测试

| 套件 | 项数 | 覆盖 |
|---|---|---|
| 解锁算法 | 28 | AES/TEA 标准向量、NCM/QMC 逐字节往返、流式跨块等价 |
| QQ 密钥 | 41 | musicex 尾部解析、密钥库、cookie 提取、重试逻辑 |
| 文件操作 | 32 | 重命名、去重、歌单匹配、安全复制 |

双环境验证：托管 Python 3.12.13 ✓ / 生产 Python 3.11.9（Qt 6.11）✓

### 设计与实现原则

1. **源文件只读**：全程不动原文件
2. **临时文件零容忍**：所有临时文件有 finally 兜底 + 启动清扫（82.7GB 泄漏事故的教训）
3. **单文件失败不中断整批**：批量操作单条失败转错误行继续
4. **取消语义**：协作式取消，取消≠失败，不留垃圾

---

## 八、写在最后

v1.1.0 的核心交付是**加密音乐格式解锁的完整链路**——从算法实现到密钥导入到标签补齐到流式优化到 UX 打磨，每个环节都做了工程化处理。

这个项目的出发点是解决我自己遇到的实际问题：曲库里混进假无损很烦，加密格式转来转去很烦，所以我把它们都自动化了。现在开源出来，希望对有同样困扰的人有用。

源码在 GitHub 上完全公开，有能力的可以自己改、自己编译。不会用 GitHub 的，发邮件给我，我直接把打包好的版本发你。

📧 feifeifynn@qq.com

业余项目，回复可能慢但一定回。

---

**祝你的曲库里，没有假无损。** 🎧
