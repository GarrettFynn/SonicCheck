# SonicCheck v1.1.0 发布说明

> 本版在 v1.0.0（无损真伪鉴定 + 曲库管理）基础上，新增**加密音乐格式解锁**完整链路，并完成一轮稳定性与安全性加固。

---

## 🎵 新功能：格式解锁（ncm / mflac / qmc → flac / wav）

- **网易云 .ncm** 与 **QQ 音乐 QMC 全系**（.mflac / .mgg / .qmc* / .bkc* 等）一键解锁为标准音频，源文件只读不动
- 输出格式「自动」保留内层原始格式（推荐）；可强制 FLAC / WAV（有损内层会明确提示"换容器不提升音质"）
- 自动写入歌名/歌手/专辑标签与封面（ffmetadata 方案，中文无乱码）

### QQ 音乐新版加密（musicex / STag）支持

- 新版客户端下载的文件密钥不在文件内，需先点「**导入 QQ 音乐密钥**」：
  软件从**运行中的 QQ 音乐客户端**只读提取登录态，调官方接口逐首换回解密密钥，
  缓存到本机密钥库（`~/.soniccheck/qqmusic_ekeys.json`）
- 窗口内置「**教程**」入口，图文引导完整流程
- 解锁后自动**联网补齐标签与封面**（按 song_mid 精确取详情，可勾选关闭走纯离线）
- 密钥导入成功后**自动接着开始解锁**（可取消勾选）

### 大文件与进度

- **流式解密**：8 MB 分块处理，实测 296 MB 文件峰值内存约 76 MB（旧方案约需 890 MB），
  内存占用与文件大小解耦
- 状态栏显示**单文件字节级进度**百分比

## 🛡 稳定性修复

- 解锁/密钥导入**全程可取消**：流式解密与 ffmpeg 转码中途可中断，关窗不再假死；
  取消产生的半截文件自动清理
- ffmpeg 转码失败/超时**不再留下半截输出文件**；超时上限按文件大小缩放
  （300 MB hi-res 不再误杀），超时与失败报错文案区分
- 扫描停止后，「等待/分析中…」的行正确标记为**已取消**，不再悬挂
- 非安全模式去重后，被清除项**从结果表移除**（与安全模式行为一致），
  避免后续重命名误操作 `_待清除/` 里的文件
- 歌单文件名模糊匹配加最小长度阈值，降低短歌名误配
- 解锁列表行完成后保留 `[NCM]` / `[QMC]` 格式前缀

## 🔐 安全加固

- QQ 音乐登录 cookie 落盘改用 **Windows DPAPI 加密**（仅当前 Windows 用户可解，
  换机/换用户自动失效回退重新导入）

## 🧪 工程

- 测试扩充至 100+ 项断言（解锁逐字节比对、跨块流式等价、密钥库、文件操作回归），
  `python tests\run_all.py` 一键全跑
- 新增 `build.bat` 一键打包与 `BUILDING.md` 打包文档

## ⚠️ 使用边界

- 解锁功能仅用于你**已合法获得（已购买/已下载）**的文件的本地格式转换，请勿用于侵权用途
- QQ 音乐新版 STag 格式（密钥完全不离线）无法本地解密，需旧版客户端重新下载

---

**升级建议**：v1.0.0 用户直接覆盖解压即可；QSettings 配置（窗口位置、标记自定义、安全模式）自动保留。
