# SonicCheck v1.0.0 —— 声鉴·曲库管家

> **一句话介绍**：拖进一个音乐文件夹，自动扫出所有"假无损"——那些看起来是 FLAC、其实是 MP3 转过来的"赝品"。

---

## 一、这玩意儿是干嘛的？（30 秒看懂）

你肯定见过这种情况：下载了一个 `.flac` 文件，体积几十 MB，看着像无损，但音质总觉得怪怪的。

**真相可能是**：它确实是 FLAC 格式，但源头是从 320kbps MP3 转过来的。FLAC 只是"容器"，里面装的东西早就被有损压缩阉割过了。这种文件叫**假无损**——格式是无损的，内容不是。

SonicCheck 做的就是：**自动批量检测你的整个曲库，把假无损揪出来**，告诉你哪首是假的、为什么是假的，还能自动给文件名打上标记。

支持格式：**FLAC、APE、WAV、AIFF**，以及同目录下的 `.lrc` 歌词文件自动关联。

---

## 二、为什么做这个？

我攒了大概 200G 的 FLAC，来源很杂 —— PT 站、资源群、各种论坛。某天用 foobar2000 的频谱插件点开一首"无损"，发现 16kHz 之后直接一刀切，典型的 MP3 假无损。那一刻血压真的上来了。

手动一首首检查不现实。网上的检测工具要么纯命令行（对小白不友好），要么功能太单薄（只告诉你 yes/no，不解释原因）。

干脆自己写一个带界面的，用 Python + PyQt6 + numpy + ffmpeg，打包成一个 4.43MB 的 exe，没装 Python 的朋友也能直接用。

---

## 三、开源声明与许可证

**本项目是开源软件，以 GNU General Public License v3.0（GPL v3）发布。**

> 为什么选 GPL v3？因为本项目使用了 PyQt6 作为 GUI 框架，而 PyQt6 采用 GPL v3 许可证。根据 GPL 的"传染性"条款，使用了 GPL 组件的项目也必须以 GPL 发布。如果你Fork后想闭源商用，需要把 PyQt6 替换成其他许可证兼容的框架（如 PySide6/LGPL），或者购买 Riverbank Computing 的 PyQt6 商业许可证。

**源码地址**：`https://github.com/GarrettFynn/SonicCheck`

你可以自由使用、修改、分发本软件，但必须遵守 GPL v3 的要求：如果分发修改后的版本，必须同样以 GPL 开源，并保留原始版权声明。

**注意**：当前仓库根目录尚未放置 `LICENSE` 文件，正式发布前请补充 [GPL v3 许可证全文](https://www.gnu.org/licenses/gpl-3.0.txt)。

---

## 四、功能总览（详细版）

### 4.1 核心功能：真伪鉴定

**怎么用**：把文件夹拖到窗口上，或者点"选择文件夹"按钮，点开始扫描。软件会递归扫描子文件夹里的所有音频文件。

**扫描完你会看到一张表格**，每一行是一首歌，列的信息很全：

| 列名 | 说明 |
|---|---|
| 状态 | ● 真无损 / ○ 假无损 / ! 分析失败 |
| 文件名 | 原文件名 |
| 评分 | 0-100 的综合质量评分 |
| 格式 | FLAC、APE、WAV 等 |
| 采样率 | 44100 Hz（CD 标准）、48000、96000 等 |
| 位深 | 16bit 还是 24bit |
| DR | 动态范围（Dynamic Range），后面会解释 |
| 截止频率 | 音频能量衰减到 -60dB 时的频率点 |
| 峰值 | 最大音量，单位 dBFS |
| 时长 | 歌曲长度 |
| 路径 | 完整文件路径 |

**鼠标悬停**在"状态"列上，会弹出 tooltip 告诉你判定原因，比如"16.2kHz 处检测到断崖式衰减"、"动态范围过低"等。

**双击某一行**可以打开详情对话框，看到这首歌的所有分析指标。

### 4.2 六维评分体系（分数怎么来的）

每首歌会有一个 0-100 的综合评分，由六个维度加权计算：

| 维度 | 权重 | 说明 |
|---|---|---|
| 位深 | 15分 | 24bit > 16bit。位深越高，量化噪声越低 |
| 采样率 | 10分 | 96kHz/192kHz 满分，44.1kHz/48kHz 也够用（CD 标准）|
| -60dB 截止频率 | 25分 | **核心指标**。高频有没有被一刀切？ |
| 动态范围 DR | 25分 | **核心指标**。音乐是动态的还是有气无力？ |
| 立体声相关性 | 15分 | 左右声道的相似度，假无损往往左右声道高度相似（有损编码的副作用）|
| 峰值 | 10分 | 有没有削波（音量爆掉）|
| 假无损惩罚 | -20分 | 一旦被判定为假无损，总分直接扣 20 分 |

### 4.3 假无损的三条判定规则（满足任意一条即判假）

**规则 1：断崖检测（最重要）**

在 15kHz 到 Nyquist 频率（采样率的一半）之间，如果频谱出现"悬崖"——相邻两个频率窗的能量落差超过 30dB，而且悬崖后面的 50 个频率点平均能量低于 -70dB——这就是典型的 MP3/AAC 编码痕迹。真正的无损音频，频谱应该是平滑衰减的，不会突然断掉。

**规则 2：-60dB 截止频率 < 16kHz**

从最高频率往低频扫，找到能量第一次跌到 -60dB 以下的那个频率点。如果这玩意低于 16kHz，说明高频信息严重缺失。CD 抓轨的真无损，这个值一般在 20kHz 以上。

**规则 3：24bit 但 DR < 4**

有些不良资源会把 16bit 的音频强行封装成 24bit（体积变大，看起来更"高级"），但实际动态范围还是 16bit 的水平。如果标称 24bit 但 DR 测出来不到 4，说明位深造假。

> 这些阈值不是拍脑袋定的，参考了 Hydrogenaudio 论坛的技术帖和 What.CD 时代的检测标准，也经过了我自己大量样本的实测校准。

### 4.4 导出 CSV

扫描完点"导出 CSV"，可以选范围：
- 全部导出
- 只导出我选中的行
- 只导出假无损（方便你集中处理"问题歌曲"）
- 只导出真无损

导出的 CSV 用 **UTF-8-BOM 编码**，Excel 直接打开中文不会乱码。文件名自动生成：`AudioQuality_YYYYMMDD_HHmmss.csv`。

### 4.5 智能重命名

扫描完后可以一键给文件名加质量标记，比如：
- `周杰伦 - 晴天 [真无损].flac`
- `周杰伦 - 晴天 [假无损].flac`

标记的文字和位置（前缀还是后缀）可以自定义。软件会先智能剥离文件名里已有的旧标记，防止套娃（不会出现 `[真无损][真无损]` 这种尴尬情况）。

**同步规则**：
- `.lrc` 歌词文件 → **自动同步改名**（因为歌词和音频是一套的）
- `.cue` 分轨文件 → **不会动**（cue 文件里写了音轨文件名，改了会破坏索引）

### 4.6 标签改名（整理文件名神器）

适合处理从网上扒拉的、文件名乱七八糟的歌曲。软件会读取音频文件内部的 metadata（ID3/Vorbis Comment），然后按规则重命名：

- 默认格式：`歌名 - 歌手.flac`
- 可选格式：`歌手 - 歌名.flac`

**如果某首歌没有标签信息，会自动跳过**，不会强行改成一个空名字。

**安全机制**：音频文件和同名 `.lrc` 歌词文件会一起处理。如果目标文件名已经存在，会**跳过而不是覆盖**（曾经出过 lrc 单独被改成别人目标名的张冠李戴 bug，已修）。

### 4.7 去重与对比（曲库瘦身）

曲库大了，重复文件是难免的。提供两种去重方式：

**方式 A：同名对比**

把文件名规范化（忽略大小写、忽略 `_` `-` ` ` 差异）后分组。比如 `周杰伦-晴天.flac`、`周杰伦_晴天.FLAC`、`周杰伦 晴天.flac` 会被当成同一组。你可以对比同组里的文件，决定留哪个。

**方式 B：内容签名去重（更猛）**

不管文件名和标签怎么变，只要音频内容本质一样，就能揪出来。

原理：提取每个音频的内容指纹——时长、DR、立体声相关度、截止频率。如果两个文件的这四个指标都在容差范围内（时长 ±0.3s、DR ±0.2、相关 ±0.01、截止 ±50Hz），就认为它们是同一首歌。

用**并查集（Union-Find）**算法做分组，几千首歌秒级处理。

**清除操作**：去重模式下，不要的文件会被移到 `_待清除/` 文件夹（**不是永久删除**），同时生成 `.restore_map.json` 记录原始路径。哪天反悔了，一键还原。

> 对比表里，每组的"处置行"可以手动点选保留哪一首。默认自动保留评分最高的那个。

### 4.8 歌单导入（我最爱用的功能）

你有一张网易云歌单，想把自己曲库里对应的歌曲挑出来建一个本地文件夹？这个功能就是干这个的。

**支持三种方式**：

1. **本地歌单文件**：`.md`、`.csv`、`.txt` 格式的歌单
2. **手动粘贴**：直接粘贴歌单文本（支持 tab 分隔，从网页表格复制过来就行）
3. **网易云链接解析**：粘贴网易云歌单链接，**不需要登录，公开歌单就能全量解析**

**匹配逻辑**（两级）：
1. 先按标签精确匹配：歌单里的"歌名 - 歌手" vs 你曲库里音频文件的 title/artist 标签
2. 标签匹配不到，再用文件名包含匹配：歌单里的歌名是否出现在某个文件名中

匹配到的文件会**复制**（不是移动，原文件不动）到 `目标文件夹/<歌单名>/` 下面。重复执行不会重复复制，是幂等的。

**未匹配的歌曲**会生成一个 `未匹配清单.txt`，同时**自动复制到剪贴板**，方便你手动去补。

> **网易云解析原理**：网易云的 v6 接口匿名请求时，`tracks` 字段只给前 10 首（限流），但 `trackIds` 是**完整**的。所以策略是：先拿完整 trackIds，再分批（每批 200 首）调 song/detail 接口补全信息。整个过程不需要登录。私密歌单不行，会提示你手动粘贴。

### 4.9 安全模式（新手保险）

左下角有个"安全模式"复选框。勾选后，**所有写操作都变成"复制"或"只读"**：

- 重命名 → 变成"复制成新文件名"，原文件不动
- 标签改名 → 同上，复制产出
- 去重清除 → 只生成索引报告，**不碰原文件**
- 歌单导入 → 副本放在 `<文件夹名>_安全输出/` 里

安全模式下如果目标文件已存在，**不会覆盖**，直接跳过。适合第一次用、心里没底的人。确认没问题了再关掉安全模式执行正式操作。

### 4.10 界面交互细节

- **拖拽支持**：直接把文件夹拖到窗口上就能扫
- **排序**：点表头可以按任意列排序，再点一次切换升序/降序
- **多选**：Ctrl 点选多行，Shift 选连续区域
- **右键菜单**：选中行后右键 → "打开所在文件夹" / "复制完整路径"
- **扫描中锁定**：扫描时"选择文件夹"按钮和拖拽区会被禁用，防止误操作
- **新手引导**：第一次启动会弹一个非模态的引导窗口（不会阻塞操作），分"使用指南"和"迭代计划"两页

---

## 五、真假无损是怎么识别的？（通俗技术详解）

这一节写给想知道原理的人。我会尽量不用公式，用大白话讲清楚。

### 5.1 核心思想：有损压缩会"吃掉"高频信息

音乐文件本质上是一串数字，记录的是声波在每个时间点的振幅。

**无损格式**（FLAC、WAV）做的是"无损压缩"——就像 ZIP 压缩文件，体积变小了，但解压后每个字节都和原来一样。如果源头是 CD 抓轨，那 FLAC 里的数据和 CD 上的数据完全一致。

**有损格式**（MP3、AAC、OGG）则不一样。它的目标是在尽量不影响听感的前提下，把文件压得更小。它会利用"人耳心理学模型"，丢掉一些"人耳不太敏感"的信息——其中最主要的就是**高频信息**。

具体来说：
- MP3（CBR 320kbps）一般会把 20kHz 以上的信息全部丢掉，16kHz~20kHz 之间也会大幅衰减
- 更低码率的 MP3（192kbps、128kbps） cutoff 频率更低，可能 15kHz 甚至 12kHz 以上就没了

**关键来了**：如果你把一个 MP3 文件转成 FLAC，FLAC 格式本身不会损失数据，但它只能保存 MP3 里**已有的**数据。MP3 已经丢掉的高频信息，不会因为转成 FLAC 就"长回来"。

所以，假无损的 FLAC 文件里，频谱会在某个频率点突然断掉——就像悬崖一样。而真无损的频谱是平滑的，逐渐衰减的。

### 5.2 软件是怎么"看"频谱的？

人眼看频谱图能发现断崖，但软件不能"看图"，它要做的是**定量分析**。

**第一步：统一解码**

不管原始文件是 FLAC、APE、WAV 还是 AIFF，软件都会先用 ffmpeg 把它们转成同一种格式：48kHz 采样率、32bit 深度的原始 PCM 数据。这样做的好处：
- 所有文件走同一条分析代码，结果可比
- 修复了一个 WAV 文件的 bug（某些 WAV 直接读会算错 Nyquist 频率）
- 用 48kHz 统一采样率，避免 44.1kHz 和 48kHz 文件的分析参数不同

**第二步：做 FFT（快速傅里叶变换）**

PCM 数据是"时间 vs 振幅"的波形，FFT 把它转换成"频率 vs 能量"的频谱。简单说，FFT 告诉你在每个频率点上有多少能量。

软件用的是 numpy 的 `rfft`，窗口大小 4096 个采样点，每次滑动 2048 个点，加 Hann 窗平滑。这样能把整首歌分成很多小段，每小段做一次 FFT，最后取平均（或者取最坏情况）。

**第三步：频域分析**

FFT 出来的结果是线性的能量值，软件把它转成对数刻度的 dB（分贝）。然后在频域上做三件事：

**① 找 -60dB 截止频率**

从最高频率（24kHz，因为 48kHz 采样率的一半是 24kHz）往低频扫，找到第一个能量跌到 -60dB 以下的频率点。这个点就是"-60dB 截止频率"。

- 真无损（CD 抓轨）：这个值一般在 20kHz 以上
- 320k MP3 转的假无损：这个值通常在 16kHz~19kHz 之间
- 192k MP3 转的假无损：这个值可能只有 15kHz 甚至更低

**② 断崖检测**

在 15kHz 到 22.8kHz（0.95 × 24kHz）之间，滑动一个窗口，检查是否存在这样的情况：

相邻两个频率窗的能量差距 > 30dB，而且"悬崖"后面的连续 50 个频率点的平均能量 < -70dB。

这对应频谱图上的视觉效果就是：一条平滑的曲线突然垂直掉下去，然后后面一直是平的（没声音了）。

**③ 计算动态范围 DR**

DR（Dynamic Range）衡量的是音乐里"最响的部分"和"最安静的部分"之间的差距。

软件用的算法参考了 DR Meter：
- 把整首歌切成很多 3 秒左右的小块
- 计算每块的 RMS（均方根）音量
- 把所有块的 RMS 排序，取最响的 20% 和最安静的 20%
- 两者的差值就是 DR

真无损的 DR 通常在 8~14 之间（取决于音乐风格，古典可能更高，电子可能更低）。假无损因为经过了有损压缩，动态往往被压缩过，DR 可能只有 4~6。

### 5.3 立体声相关性是什么？

MP3 等编码器为了进一步压缩体积，会利用"左右声道有很多共同信息"这个特点，把左右声道合并处理（Joint Stereo）。这会导致一个副作用：解码出来的左右声道相关性变得异常高。

软件会计算左右声道的归一化互相关系数，范围是 -1（完全反相）到 1（完全一样）。真无损的这个值因音乐而异，但假无损往往异常接近 1。

### 5.4 评分的计算逻辑

六个维度分别打分，然后加权求和：

```
总分 = 位深分 + 采样率分 + 截止频率分 + DR分 + 相关分 + 峰值分
if 被判假无损:
    总分 -= 20
```

每个维度的分数不是线性的，而是按区间给的。比如 DR：
- DR ≥ 12：25 分满分
- DR 8~12：线性递减
- DR 4~8：继续递减
- DR < 4：0 分

这样设计的目的是：让"好"和"差"之间的区分度足够明显。

---

## 六、识别结果的可信度有多高？

这是大家最关心的问题："你说它是假无损，我就信吗？"

### 6.1 判"假"的可信度：非常高

如果软件判定一首歌是假无损，可信度很高。原因：

**断崖检测是物理层面的证据**。频谱在 16kHz 处突然掉下去 30dB，这不是"音质好坏"的主观评价，是**客观数据**。真正的无损音频不可能出现这种情况——CD 的 Nyquist 频率是 22.05kHz，真无损的频谱应该平滑延伸到 20kHz 以上。

**-60dB 截止 < 16kHz 也是硬指标**。如果高频信息在 16kHz 以下就彻底消失了，那这首歌的源头不可能是 CD 抓轨（CD 的频率响应上限是 22.05kHz）。唯一合理的解释是：它从有损格式转换而来。

> 换句话说，**软件判"假"的逻辑是"排除法"**：如果出现了有损编码特有的物理痕迹，那它一定不是真无损。这个逻辑是严密的。

### 6.2 判"真"的可信度：高，但不是 100%

如果软件没有检测到断崖、截止频率正常、DR 正常，会给一个"真无损"的判定。这个判定的意思是：**"我没有找到任何假无损的证据"**。

但这不等于 100% 保证它是从 CD 抓轨的。原因：

1. **理论上可以伪造频谱**：如果把一个 MP3 转 FLAC，然后人为用音频编辑软件把高频"补"上（加噪声、复制低频内容上去），频谱看起来就可能没有断崖。但这种情况在"民间资源"里极其罕见，因为这么做没有意义——既然要伪造，为什么不直接放真无损？

2. **从更高码率有损格式转的**：比如从 640kbps AAC（接近无损）转的 FLAC，高频损失可能很小，断崖不明显，截止频率也可能正常。这种情况下软件可能判"真"，但严格来说它确实不是"原始无损"。不过这种素材已经很难和真无损区分了。

3. **CD 本身的质量问题**：有些 CD 母带本身高频就不丰富，或者抓轨时出了错（光头偏移、划痕）。这些情况下真无损也可能测出较低的截止频率。但这种情况是少数。

### 6.3 总结：可信度分层

| 判定结果 | 可信度 | 说明 |
|---|---|---|
| **判假** | **> 95%** | 物理层面的证据，几乎不会冤枉 |
| **判真** | **~85-90%** | "没找到假证据"，极少数情况可能漏网 |
| **分析失败** | — | ffmpeg 读不了或文件损坏，需要人工检查 |

> 对于大多数用户来说，这套系统的实际意义在于：**快速筛选出明显有问题的那一批**。如果你的曲库里有 1000 首歌，软件挑出 50 首假无损，这 50 首你可以非常放心地处理掉。剩下的 950 首，即便有极个别漏网之鱼，对整体曲库质量的影响也微乎其微。

---

## 七、技术渊源与致谢

### 7.1 核心算法的来源

SonicCheck 的音频分析引擎不是从零发明的，而是**基于一个早期命令行脚本 `audio_compare_多进程并行版.py` 进行工程化重写**的。

具体来说：
- **判定口径逐字保留**：假无损的三条判定规则（断崖、-60dB 截止、24bit+低 DR）、六维评分体系的权重分配、断崖检测的窗口参数（>30dB 落差、50 bin 均值 <-70dB）均与原始脚本保持一致
- **数值口径不变**：迁移后通过 `dev/compare_new_vs_legacy.py` 用同批 FLAC 样本做全字段比对，diff 为 0.00e+00，证明计算结果完全一致
- **主要改动**：给原始脚本加上了 GUI（PyQt6）、多线程调度（QThreadPool）、numpy 向量化优化（替代纯 Python 循环）、ffmpeg 统一转码（修复 WAV 频率轴 bug）、以及文件管理功能（重命名、去重、歌单导入等）

如果你手里有那个原始脚本，你会发现 SonicCheck 的 `core/analyzer.py` 里的核心函数（`detect_spectral_cliff`、`compute_dynamic_range`、`score_quality`）与它的逻辑几乎一致，只是包装方式变了。

### 7.2 算法思路的社区来源

以下社区资源和开源项目为本项目的判定逻辑提供了理论基础，特此致谢：

| 来源 | 贡献 | 许可证/性质 |
|---|---|---|
| **Hydrogenaudio 论坛** | 无损音频检测的方法论讨论，特别是频谱断崖作为有损编码痕迹的判定共识 | 社区公开讨论 |
| **What.CD** | 黄金时代的音乐共享社区，其内部检测标准影响了 cutoff 阈值和 DR 容差的设定 | 已关闭社区，标准流传于社区记忆 |
| **DR Meter** | 动态范围（DR）的计算算法参考，分块 RMS 取 Top 20% vs Bottom 20% 的差值 | 算法公开，网站 https://dr.loudness-war.info/ |
| **ffmpeg / ffprobe** | 音频解码、格式转换、元数据读取（Gyan 官方 Windows 静态构建版）| LGPL v2.1+ / GPL v2+ |
| **numpy** | FFT 计算（`numpy.fft.rfft`）、向量化数值运算 | BSD 3-Clause License |
| **PyQt6** | 跨平台 GUI 框架，本项目的所有界面组件均基于此 | GPL v3 |

### 7.3 第三方依赖清单

本项目直接依赖的第三方库及工具：

| 依赖 | 版本要求 | 用途 | 许可证 |
|---|---|---|---|
| Python | ≥ 3.10 | 运行时 | PSF License |
| PyQt6 | ≥ 6.6.0 | GUI 界面框架 | GPL v3 |
| numpy | ≥ 1.26.0 | FFT 与数值计算 | BSD 3-Clause |
| ffmpeg | 任意 release | 音频解码与转码 | LGPL v2.1+ / GPL v2+ |
| ffprobe | ffmpeg 配套 | 音频元数据读取 | LGPL v2.1+ / GPL v2+ |

**打包工具**（构建时使用，运行时不需要）：
- PyInstaller（用于生成 `SonicCheck.exe`）— GPL + 特殊例外条款

> **重要**：分发本软件的二进制版本时，你必须同时提供本项目的源代码（或提供获取源码的方式），并在文档中明确声明本软件以 GPL v3 发布。这是 GPL 的要求，也是你对开源社区的回报。

---

## 八、迭代计划（Roadmap）

一期已经 release，二期想做这些：

- [ ] **迷你频谱图缩略列**：结果表格里加一列小频谱图，一眼看出高频有没有断
- [ ] **设置面板**：自定义判定阈值、评分权重、线程数持久化
- [ ] **单文件拖拽分析**：不用扫整个文件夹，拖一首进来直接分析
- [ ] **声纹去重**：用感知哈希做跨音质版本的重复识别（同一首歌的 FLAC 和 MP3 版本也能认出是同一首）
- [ ] **QQ 音乐歌单解析**
- [ ] **更多导出格式**：Excel、JSON

欢迎提 Issue 和 PR，业余项目，回复可能慢但一定会看。

---

## 九、下载

- `SonicCheck.exe`（见下方 Assets）—— 4.43MB，解压即用，无需安装 Python
- 源码：`git clone https://github.com/GarrettFynn/SonicCheck.git`

> **注意**：二进制版本（exe）本身不包含 ffmpeg。你需要自行下载 ffmpeg（见第十节），或者使用内置目录方式放置（`resources/ffmpeg/`）。

---

## 十、系统要求与安装环境

### 10.1 最低系统要求

- **操作系统**：Windows 10 或 Windows 11（64位）
- **内存**：至少 4GB（分析时会把音频解码到内存）
- **磁盘空间**：软件本身只占几 MB，但临时文件需要几百 MB 空间（放 `%TEMP%` 目录）
- **依赖**：**ffmpeg + ffprobe**（必须！软件本身不带，需要你自己装）

### 10.2 什么是 ffmpeg？为什么需要它？

ffmpeg 是一个开源的音视频处理工具，全世界都在用。SonicCheck 用它来：
- 把 FLAC/APE/WAV 等格式统一解码成原始音频数据
- 读取音频文件的标签信息（歌名、歌手等）
- 获取音频的技术参数（采样率、位深、时长等）

**ffprobe 是 ffmpeg 的配套工具**，专门用来"查看"音视频文件的信息，不修改文件。

你可以把 ffmpeg 理解为"音频文件的翻译官"——SonicCheck 不会直接读 FLAC 文件，而是通过 ffmpeg 先把 FLAC 翻译成软件能懂的原始数据。

### 10.3 安装 ffmpeg（推荐方式）

**方法 A：下载 Gyan 的 Windows 静态版（最推荐）**

1. 打开这个链接：`https://www.gyan.dev/ffmpeg/builds/`
2. 找到 "release essentials" 版本（比如 `ffmpeg-release-essentials.zip`）
3. 下载并解压到某个文件夹，比如 `D:\ffmpeg\`
4. 把 `D:\ffmpeg\bin\` 添加到系统环境变量 PATH 里
5. 打开 CMD，输入 `ffmpeg -version`，如果有输出说明装好了

**方法 B：放到软件内置目录（最简单）**

1. 下载同上
2. 解压后，把 `ffmpeg.exe` 和 `ffprobe.exe` 两个文件
3. 放到 SonicCheck.exe 同级的 `resources/ffmpeg/` 文件夹下（没有就新建）
4. 这样软件启动时会自动找到，不需要改环境变量

**方法 C：用 Chocolatey 包管理器（如果你已经装了 Chocolatey）**

```
choco install ffmpeg
```

装完就能用，会自动加 PATH。

> 如果以上方法你都不会，看下一节。

---

## 十一、不会装环境？复制这段话给 AI，让它手把手教你

把下面这段文字原样复制，发给任意一个 AI（ChatGPT、Claude、Kimi 都行），它会一步一步带你装好：

```
我是一个完全不懂技术的 Windows 小白，现在需要安装 ffmpeg 和 ffprobe，因为我下载了一个叫 SonicCheck 的软件，它需要这两个工具才能运行。

请一步一步指导我：
1. 去哪里下载 ffmpeg（给我直接可点的链接）
2. 下载哪个版本（Windows 64位）
3. 下载后怎么解压
4. 怎么把 ffmpeg 放到 SonicCheck 的 resources/ffmpeg/ 文件夹下（推荐这种方式，不用改系统设置）
5. 怎么验证是否安装成功

每一步都要非常详细，假设我完全不懂命令行、不懂环境变量、不懂任何技术术语。如果某一步需要我打开某个软件或文件夹，请告诉我具体的操作路径（比如"右键点击开始菜单"、"按 Win+E 打开资源管理器"）。

我的 SonicCheck.exe 放在这个路径：[请在这里填入你的 SonicCheck.exe 的实际路径，比如 D:\MusicTools\SonicCheck.exe]
```

> 把 `[请在这里填入...]` 那段换成你电脑上 SonicCheck.exe 的实际路径，然后发给 AI 就行。

---

**祝你的曲库里，没有假无损。** 🎧
